\section{Architecture Search Security}

Architecture search exposes structural signals and decisions in addition to conventional model updates. This theme covers integrity, confidentiality and adversarial-robustness risks introduced or addressed during search and deployment.

\subsubsection{Challenge 4.1: Poisoning the Architecture Search}

Malicious clients can manipulate model weights and architecture-search signals through poisoned data or local optimisation. The attack can therefore pursue a backdoor objective while changing operation probabilities or candidate rankings. In weight-sharing search, compromised weights can also affect every candidate that reuses them. Experiments with one malicious client among eight found substantial degradation in two discrete weight-sharing strategies under fourfold-scaled updates, showing that poisoning can persist in the selected architecture~\cite{privacy_preserving_fednas_for_edge_2025}.

\noindent\textbf{Solution 4.1a: Use a trained differentiable search state to attenuate poisoned operations.} In differentiable supernet-based search, each candidate operation remains part of a weighted mixture while clients update both its weights and selection probability. The cited analysis derives a reduction in an operation's mixture contribution when its poisoned weights provide an opposing or comparatively small gradient for the main task. This result assumes Lipschitz continuity, convexity, bounded gradients and a unique global optimum~\cite{privacy_preserving_fednas_for_edge_2025}. The evaluation supports the mechanism only for the studied backdoor attacks and search spaces. Retraining the selected fixed architecture from scratch did not preserve the reported robustness, so the evidence concerns the trained differentiable search state rather than intrinsic robustness of the architecture~\cite{privacy_preserving_fednas_for_edge_2025}.

\noindent\textbf{Solution 4.1b: Restrict the search space before federated search.} The server can evaluate modules on server-held data and select a compact set that balances predictive performance, memory and latency before clients begin differentiable search. Constraining model complexity in this way reduced overfitting to noise introduced by backdoor triggers in the cited evaluation~\cite{privacy_preserving_fednas_for_edge_2025}.

\subsubsection{Challenge 4.2: Architecture-Search Artefacts Create Additional Privacy Exposure}

NAS-in-FL communicates data-dependent signals that fixed-architecture FL does not require. Differentiable strategies can expose architecture gradients, while other strategies may transmit discrete architecture graphs, personalised choices or scalar rewards~\cite{dp-fnas_2020,multi-granular_fednas_2023,privacy_preserving_fednas_for_edge_2025,feathers_2023}. The cited literature treats local sample counts, class proportions and example reconstruction as possible attack targets rather than demonstrating their recovery~\cite{multi-granular_fednas_2023}. It also proposes that personalised architecture choices could help identify the source of an update, but does not evaluate such an attack~\cite{privacy_preserving_fednas_for_edge_2025}. These data-dependent artefacts therefore constitute a potential privacy exposure whose practical severity depends on the transmitted representation and adversary.

\noindent\textbf{Solution 4.2a: Apply differential privacy to search signals.} Differentiable strategies can clip model and architecture gradients and add Gaussian noise before upload, while scalar hyperparameter rewards can be perturbed in the same way~\cite{dp-fnas_2020,feathers_2023}. In vertically partitioned search, clipping and noise can instead protect the intermediate activations and backward gradients exchanged between parties~\cite{self-supervised_vertical_fednas_2023}. The added noise can, however, impair model and search performance~\cite{privacy_preserving_fednas_for_edge_2025}.

\noindent\textbf{Solution 4.2b: Securely aggregate a uniform shared search.} When every client updates the same differentiable supernet, model weights and architecture variables have aligned shapes and can use the same weighted secure-aggregation protocol. Personal architecture selection and adaptation can then occur locally after the shared search, preventing the server from observing each client's deployed choice~\cite{privacy_preserving_fednas_for_edge_2025}. This requires a uniform search representation during collaboration.

\noindent\textbf{Solution 4.2c: Exchange discrete architecture encodings separately from model parameters.} The graph aggregation described in Solution 3.2a can upload discrete architecture encodings while handling model-parameter training in a separate stage. It therefore constructs the next global architecture without receiving the corresponding weights or continuous gradients~\cite{multi-granular_fednas_2023}. For a seven-layer example, the cited analysis compares a 56-entry derivative matrix with the 50,176 pixels in an image batch and argues that the resulting optimisation has many possible solutions, making precise pixel reconstruction by that procedure impractical~\cite{multi-granular_fednas_2023}. This dimensionality argument does not exclude property or source inference and does not provide a formal privacy guarantee, so the data-dependent encoding may still require differential privacy or encryption.

\subsubsection{Challenge 4.3: Adversarial Examples Can Evade the Deployed Model}

Poisoning manipulates the search or training process, while an evasion attack perturbs inputs after deployment. An attacker with access to a compromised client can train a replacement model on local data and use NAS to find an architecture whose adversarial examples transfer effectively to the federated target model~\cite{fednas_based_attack_2024}. The deployed architecture therefore affects robustness to an attack that does not modify federated updates.

\noindent\textbf{Solution 4.3a: Search jointly for clean performance and adversarial robustness.} Candidate architectures can be evaluated through federated adversarial training using both unmodified and adversarial examples. Treating clean predictive performance and adversarial accuracy as separate objectives produces a set of trade-off architectures from which a deployment can be selected~\cite{fednas_based_attack_2024}. The cited evidence concerns intrusion detection and specific transfer attacks, so robustness to other domains and adversaries requires separate evaluation.
