% NAS explanation and why it is important
Deep learning has seen widespread adoption over the last decade and is being applied to an increasingly diverse set of tasks. Applying deep learning traditionally involves a team of deep learning and domain experts who tailor a neural network architecture to a specific task through a lengthy process of trial and error. To automate this process, researchers invented \textit{Neural Architecture Search} (NAS)~\cite{nas_survey_2019} methods. NAS methods employ diverse strategies to automatically search for a neural network architecture for a given deep learning task within an architecture search space.

% FL explanation and why it is important
Independently, but in parallel to NAS, researchers developed a machine learning approach called \textit{Federated Learning} (FL)~\cite{fl_seminal_2017}, which enables training an ML model on data distributed across a set of clients without sharing their data. The model is distributed from the server to the clients, which train the model on their local data. The clients then send the model weight updates to the server, which aggregates them and redistributes them to the clients. This process is repeated for several rounds. FL enhances the privacy of client data and enables training even when clients' data is too large to transfer effectively to a central training site or cannot be shared due to regulatory or privacy constraints.

There has been significant interest in using NAS in FL over the last couple of years, but using NAS methods in FL is not straightforward. Most NAS methods were developed for a centralised setting and rely on assumptions that often do not hold for FL. These assumption discrepancies create several \textit{challenges} for using NAS in FL. For example, in a centralised setting, practitioners can expect a NAS method to run on worker nodes connected via low-latency, high-bandwidth links. However, in FL, a NAS method may run on clients connected via an unreliable, high-latency, low-bandwidth network. This creates a challenge when transferring the weights of large candidate models between clients and the server to coordinate an architecture search. Practitioners need specialised solutions to overcome such challenges, such as only transferring encodings of candidate architectures to clients \cite{aging_fednas_2025}.

The NAS-in-FL literature contains many NAS-in-FL methods that propose various solutions for overcoming the challenges of using NAS in FL \cite{nas_in_fl_2026}. As NAS-in-FL research has progressed, several overarching challenges and solution themes have appeared in the literature. Despite differences in implementation and terminology, solutions are often conceptually similar. For example, different methods use partial-parameter aggregation or knowledge transfer to coordinate searches involving heterogeneous architectures \cite{fedoras_2022,rafl_2024,hapfnas_2025}. 

Earlier reviews classify methods as online or offline and single- or multi-objective, summarise a small set of NAS-in-FL methods within a broader NAS and hyperparameter optimisation review, or discuss NAS as one part of multi-objective FL \cite{fl_to_nas_survey_2021,nas_hpo_fl_survey_2023,multi-objective_methods_in_fl_2025}. More recent work supports comparison by conceptualising a generic NAS-in-FL pipeline, design elements, and method archetypes \cite{nas_in_fl_2026}. While these reviews provide an overview of the types of NAS-in-FL methods, they do not highlight individual challenges of NAS-in-FL methods and the solutions covered in the literature to overcome them. NAS-in-FL research lacks a concept-centric overview of its problem and solution space. Individual studies describe challenges and solutions for particular NAS methods and FL settings using different terminology and levels of abstraction, but they do not reveal which challenges and solutions recur across the literature as overarching concepts.

% TODO: what is the confusion, how do we help create clarity?
% NAS-in-FL is an emerging field, but the challenge-solution space remains underspecified
% It's clear that NAS-in-FL is difficult, but the reasons why it is difficult are murky
% What makes NAS-in-FL challenging, how is it challenging, how can we overcome these challenges?

%This thesis contributes a component-level synthesis of NAS-in-FL design knowledge. It decomposes complete NAS-in-FL methods into recurring challenge and solution mechanisms, maps their many-to-many relationships, and retains the conditions, dependencies and trade-offs under which the literature presents these mechanisms as applicable. This decomposition complements method-level classifications by showing how solution components can be combined into configurations for different federated conditions.

% contributions:
% - conditions under which those relationships apply
% - recurring challenges that are described differently across papers
% - dependencies and trade-offs between solution components
% - recurring challenges that are described differently across papers
% - reusable solution mechanisms that recur within otherwise different methods, i.e. they are backed by the literature
% - conditions under which those relationships apply
% - claim is NOT "if these characteristics, this is the solution", but more like "if these characteristics, solutions already exist for challenges"

% challenges remain underspecified

% Research Problem:
% - Under what circumstances do which challenges arise?
% - What are the reasons that NAS in FL is hard?
% - How has the literature dealt with these challenges so far?

While themes in challenges and solutions have emerged, they can not be applied universally, since they depend on the FL system characteristics and the used NAS-in-FL method. Furhtermore, seemingly related challenges are quite different upon closer inspection and warrant different solutions. A clear overview of the challenge and solution space for NAS-in-FL is needed to make practitioners aware under which circumstance which potential pitfalls occur and make them aware for which circumstances solutions already exist and where work can be done. This leads us to ask the following research question:

% Gap:
% - recurring NAS-in-FL scenarios have not been consolidated  
% - challenges are described using inconsistent terminology  
% - recurring solution mechanisms have not been synthesized  
% - scenario–challenge–solution relationships remain unclear

% Most current research question candidates
\textit{What solutions exist for challenges in NAS-in-FL scenarios common in the literature?}
\textit{How does the literature relate common NAS-in-FL scenarios to challenges and their proposed solutions?}

% TODO: add adjective that allows room for discussion
% adjectives: recurring/common
% or maybe recurring challenges?
% single short, research question that asks one question in one sentence (no cheating with commas and coupling sentences)
\textit{Which <adjective placeholder> solutions does the literature propose for addressing the challenges that arise when using NAS in FL?}

% # "Study: How investigate? Process, context & why?"
% ## "Indicate that this study addresses that problem or issue and state how."
% ## "Describe the study, sample, and method for addressing that problem or issue."
To tackle our research question, we conduct an extensive systematic literature review. We collect an initial set of relevant literature with a Scopus~\cite{scopus} search and extend it with a snowball search. We first use open coding to identify, from the literature, assumption discrepancies and resulting challenges, when using NAS in FL. Next, we extract FL system characteristics and use axial coding to code the influence of characteristics on the relevance of challenges. Then, we extract techniques through open coding and iteratively refine them to synthesise a coherent set of mutually exclusive and collectively exhaustive techniques. Finally, we use axial coding to map techniques onto challenges they overcome.

% # "Conclusion"
% ## "Describe what you found. State explicitly how these findings extend and contribute to existing knowledge."
Our review aims to synthesise techniques for using NAS in FL from the literature to make finding suitable techniques easy in practice. We make the following three contributions. First, by providing a consolidated overview of the challenges of using NAS in FL, practitioners can grasp which issues they can expect to encounter at a glance. Second, by describing how FL system characteristics influence the relevance of challenges, we enable practitioners to focus on the challenges relevant to them. Third, by extracting techniques for using NAS in FL and highlighting the challenges they address, we enable practitioners to compare existing techniques for using NAS in FL for their specific set of challenges.

% The outline paragraph (at `./03_outline.tex`) isn't included here, as it is not required by the exposé (it would be redundant due to the "Structure" section in the exposé). 
% It is included in the thesis document via `../../main.tex`.