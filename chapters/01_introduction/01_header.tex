% !TeX root = ../main.tex
% Add the above to each chapter to make compiling the PDF easier in some editors.
\chapter{Introduction}\label{chapter:introduction}

% This chapter is in a separate file sothat `./02_main_content.tex` can be included in the exposé at `../../expose/expose.tex`. This is because the `\chapter` macro is not defined by the document class used by the exposé.